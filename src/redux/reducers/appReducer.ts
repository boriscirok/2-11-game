import { AppActions } from '../actions/appActionsTypes'

export type State = {

}

export const initialState: State = {

}

export function appReducer(state: State = initialState, action: AppActions) {
  return state
}
